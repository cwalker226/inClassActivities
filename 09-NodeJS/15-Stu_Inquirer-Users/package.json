{
  "name": "15-stu_inquirer-users",
  "version": "1.0.0",
  "description": "build a simple command line application that accepts user input and writes it to a `.json` file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Corey",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^7.1.0"
  }
}
